// 方法工具函数统一入口
import {formatRichText, formatPrice, sliceWord} from './common'
import {areaData, provinceCityData} from './area'

export {
    formatRichText,
    formatPrice,
    sliceWord,
    areaData,
    provinceCityData
}
